# API base URL (server + client)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Clerk — replace with real keys from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace
CLERK_SECRET_KEY=sk_test_replace
CLERK_WEBHOOK_SIGNING_SECRET=whsec_replace

# Mode switch: true = local demo (Clerk bypassed), false = real Clerk auth
NEXT_PUBLIC_MOCK_MODE=false
MOCK_MODE=false
